# Pretrained weights

Pretrained weights are not yet available. Download instructions will be added here with the checkpoint release.

`manifest.json` currently contains placeholder metadata. The checkpoint downloader will remain unavailable until the release details are filled in.
